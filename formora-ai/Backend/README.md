# Formora AI backend

This local server keeps the OpenAI API key outside the Chrome extension.

1. Set `OPENAI_API_KEY` and optionally `PORT` in your terminal environment.
2. Run `npm start` in this folder.
3. Reload the Formora extension, open a webpage, then select **Analyze page**.

The server listens on `http://localhost:8787` by default.
